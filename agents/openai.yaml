interface:
  display_name: "动态表情包生成器"
  short_description: "从角色参考与主题制作统一风格的动态 GIF 表情包"
  default_prompt: "Use $animated-sticker-generator to create a consistent animated sticker pack from my character reference and theme."
policy:
  allow_implicit_invocation: true
